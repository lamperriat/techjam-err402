# Competition Data

The catalog is an organizer-provided artifact and is intentionally not bundled.
Download `catalog.jsonl.gz` from the competition GitHub Release and decompress it
to `data/catalog.jsonl` before running an agent locally.

Never place API keys or private evaluation data in this directory.
